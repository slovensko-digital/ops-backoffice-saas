name: PROD Vrakuňa Deploy

on:
  workflow_call:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: prod_vrakuna
      environment_url: https://vrakuna.odkazprestarostu.sk

    secrets: inherit
