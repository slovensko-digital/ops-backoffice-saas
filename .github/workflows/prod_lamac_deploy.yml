name: PROD Lamač Deploy

on:
  workflow_call:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: prod_lamac
      environment_url: https://lamac.odkazprestarostu.sk

    secrets: inherit
